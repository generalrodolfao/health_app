# Database
DB_USER=healthapp
DB_PASSWORD=changeme-str0ng-pass-here
DB_NAME=healthapp

# JWT - generate with: openssl rand -hex 32
JWT_SECRET=generate-one-with-openssl-rand-hex-32

# Domain (your VPS domain or IP)
DOMAIN=healthapp.example.com
LETSENCRYPT_EMAIL=admin@healthapp.example.com

# Docker images (optional: use GCR images from CI)
# BACKEND_IMAGE=ghcr.io/your-user/health_app/backend:latest
# FRONTEND_IMAGE=ghcr.io/your-user/health_app/frontend:latest